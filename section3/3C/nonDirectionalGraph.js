/**
 * 
 * Write a program that counts the number of nodes in a non-directional graph and finds out whether there is a cycle in the graph

Notes: an adjacency list is faster at iterating over all edges of a graph.
 */

class Graph {
    constructor() {
        this.adjacencyList = {}
    }
}

const list = {
    vertex1: ['such'],
    vertex2: ['a'],
    vertex3: ["mess"]
}